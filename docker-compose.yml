version: '3.3'
services:
  frontend:
    build:
      context: "./frontend"
      target: 'develop-stage'
    ports:
      - '8080:8080'
    volumes:
      - './frontend:/app'
    command: /bin/sh -c "yarn serve"